# ClusterIP only: the browser reaches the API through nginx's /api/ proxy in the ui pod.
apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: healthwatcher
spec:
  selector:
    app: api
  ports:
    - port: 8080
      targetPort: 8080
