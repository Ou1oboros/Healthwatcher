# NodePort, so the dashboard opens from the host browser with no Ingress controller.
apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: healthwatcher
spec:
  type: NodePort
  selector:
    app: ui
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
